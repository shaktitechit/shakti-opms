# Add inside the http { } block of /etc/nginx/nginx.conf (once).
#
#   include /etc/nginx/snippets/opms-http-map.conf;
#   client_max_body_size 50m;

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
