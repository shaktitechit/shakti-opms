# Hard 15-minute IP ban after repeated failed / rate-limited logins.
# Deploy on the nginx host:
#   sudo cp nginx/fail2ban/filter.d/opms-auth-login.conf /etc/fail2ban/filter.d/
#   sudo cp nginx/fail2ban/jail.d/opms-auth.conf /etc/fail2ban/jail.d/
#   sudo fail2ban-client reload
#
# Requires: auth-login.access.log (see snippets/auth-rate-limit.conf)

[opms-auth-login]
enabled  = true
port     = http,https
filter   = opms-auth-login
logpath  = /var/log/nginx/auth-login.access.log
backend  = auto
maxretry = 20
findtime = 15m
bantime  = 15m
action   = %(banaction)s[name=%(__name__)s-tcp, port="%(port)s", protocol="tcp", chain="%(chain)s"]
