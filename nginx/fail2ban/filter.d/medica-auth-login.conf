# Fail2ban filter for Medica auth login abuse (medica-auth-login.access.log).
# Matches 401 / 429 on POST /api/auth/login (opms_main log format).

[Definition]
failregex = ^<HOST> auth\.medicaent\.in POST /api/auth/login (401|429)\b
ignoreregex =
