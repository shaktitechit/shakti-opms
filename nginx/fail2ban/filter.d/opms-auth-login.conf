# Fail2ban filter for OPMS auth login abuse (nginx auth-login.access.log).
# Matches 401 / 429 on POST /api/auth/login (opms_main log format).

[Definition]
failregex = ^<HOST> auth\.spspl\.com POST /api/auth/login (401|429)\b
ignoreregex =
