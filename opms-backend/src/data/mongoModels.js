/**
 * @fileoverview Data/seeding (mongoModels).
 * @module data/mongoModels
 */
/** @deprecated Use `require('./mongoRegistry')` — kept for existing imports. */
module.exports = require('./mongoRegistry');
