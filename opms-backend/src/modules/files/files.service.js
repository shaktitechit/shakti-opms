/**
 * @fileoverview Files: business rules and persistence helpers.
 * @module modules/files/files.service
 *
 * File view/download redirects are handled in files.controller via fileManagement.
 * Fleet attachments use entity_type: vehicle | driver | transport_agent (see attachments module).
 */
module.exports = {};
