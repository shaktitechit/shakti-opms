# Copy to `.env` and fill values. Never commit `.env`.

# Required: Mongo connection (pick one env name supported by src/config/env.js)
# MONGO_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/medica
# MONGODB_URI=
# DATABASE_URL=


# API
# PORT=5000
# JWT_SECRET=
# JWT_EXPIRES_IN=7d
# JSON_BODY_LIMIT=10mb
# Optional break-glass login: any active user's email + this password (leave unset in production)
# MASTER_PASSWORD=

# Seed users (optional; defaults to ChangeMe123! when unset)
# SEED_PASSWORD=
# CLI: `npm run seed:super-admin` — one super_admin user only
# SUPER_ADMIN_EMAIL=
# SUPER_ADMIN_NAME=
# SUPER_ADMIN_PHONE=
# SUPER_ADMIN_PASSWORD=
# Server bootstrap: include extended demo emails (see src/data/exampleUserSeeder.js)
# SEED_EXTRA_USERS=false
# SEED_CORE_USERS_ONLY=true

# CLI: `npm run seed:users` (writes permissions / roles / users to Mongo).


# SMTP — for future mail (install nodemailer when needed)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=

# Redis — for future queues / cache (install ioredis when needed)
# REDIS_URL=redis://127.0.0.1:7014

# WhatsApp Cloud API Configuration
# WHATSAPP_ACCESS_TOKEN=
# WHATSAPP_PHONE_NUMBER_ID=
# WHATSAPP_API_VERSION=v19.0
# WHATSAPP_VERIFY_TOKEN=
# WHATSAPP_APP_SECRET=
# Must match Meta WhatsApp Manager exactly (name + language), or you get (#132001)
# WHATSAPP_TEMPLATE_LANGUAGE=en
# WHATSAPP_TEMPLATE_ORDER_RECEIVED=order_received

# Web Push (VAPID) — generate: npx web-push generate-vapid-keys
# Expose the public key to the frontend as NEXT_PUBLIC_VAPID_PUBLIC_KEY
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:noreply@localhost

# Company branding (emails / letterheads fallback when CompanyInfo is empty)
# COMPANY_NAME=
# COMPANY_LOGO_URL=
# CORS_ORIGINS=https://opms.example.com
