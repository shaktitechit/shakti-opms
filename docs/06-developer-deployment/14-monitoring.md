# Monitoring

| Capability | Status |
|------------|--------|
| `/health` endpoints | Implemented on backends |
| Redis healthcheck | Compose `redis-cli ping` |
| Prometheus/Grafana/APM | **Not identified** |
| Uptime checks | **Not identified** in repo |
| Alerting | **Not identified** |

SSE streams exist for notifications — not a metrics system.
