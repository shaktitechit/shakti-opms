# Git Workflow

## Observed
- Git repository at monorepo root
- `.gitignore` excludes `.env.docker`, backups, minio-data, etc.

## Formal branching / PR policy
**Not identified** in repository (no CONTRIBUTING.md).

**Requires confirmation** from team process.
